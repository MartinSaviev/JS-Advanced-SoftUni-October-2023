function extractText() {

    let inputElements = document.getElementById('items');

    let resultElements = document.getElementById('result');

    resultElements.textContent = inputElements.textContent;

}